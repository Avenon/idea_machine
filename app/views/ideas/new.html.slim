h2 = t :add_idea
.row
  .col-md-5
  .col-md-2
    = form_for(@idea) do |f|
      = f.label (t :add_idea)
      = f.text_area :description, class: "form-control"
      = f.hidden_field :user_id, value: current_user.id
      br
      = f.submit (t :idea_create), class: "btn btn-default btn-block"
